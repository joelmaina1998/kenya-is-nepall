# Kenya Is Nepal — Transfer Package Status

## GitHub

- Repository: https://github.com/joelmaina1998/kenya-is-nepal
- Repository status: created and ready for the first push
- Push status: pending until the clean commit is created locally

## Vercel

- Status: not deployed from this environment
- Vercel setup: requires the Vercel integration or a user-authenticated Vercel dashboard session
- Safe deployment instructions: `transfer-package/VERCEL_DEPLOY_STEPS.md`

## Replit

- Current Replit deployment URL: not verified in this project
- The development app was verified locally at `/`, `/setup`, `/admin`, `/api/config`, `/api/stats`, and `/api/setup/schema`.

## What remains

1. Commit and push the clean project to GitHub.
2. Connect Vercel, import `joelmaina1998/kenya-is-nepal`, and deploy with the four environment variables.
3. Run the Supabase schema, create the Auth administrator privately, and create the public storage bucket.
4. Add `kenyaIsNepal.org` in Vercel Domains and complete DNS verification.

## Security note

Supabase secret values and administrator credentials are intentionally not stored in this file, the repository, or the transfer ZIP. Add them through Replit Secrets or Vercel's environment-variable UI.